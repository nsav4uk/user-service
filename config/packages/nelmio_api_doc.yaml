nelmio_api_doc:
    documentation:
        info:
            title: User Service
            description: Service for create and authorize user
            version: 1.0.0
    areas: # to filter documented areas
        path_patterns:
            - ^/api(?!/doc$) # Accepts routes under /api except /api/doc
